<li>
	{{burger_name}}

	<button class="data-devoured" data-id="{{id}}" data-devoured= "{{devoured}}">
		{{#if devoured}}DEVOUR!{{else}}Eat!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">Delete!</button>
</li>